      subroutine gmsgd(iwin,msg)
c     extremely dumb routine for error messages
      character*(*) msg
      integer*2 iwin(4)

      integer nblen
      external nblen

c     write (*,*) msg(1:nblen(msg))
      continue

      return
      end
