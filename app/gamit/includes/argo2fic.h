c     the maximum number of SVs allowed in this format is MAXSVS
      integer*4 maxsvs
      parameter (maxsvs=11)

c     the maximum number of SVs allowed per session 
c     make this big to keep us out of trouble
      integer*4 maxsat
      parameter (maxsat=28)                 

c     the maximum number of ephemeris records
      integer*4 maxeph
      parameter (maxeph=1000)                 

